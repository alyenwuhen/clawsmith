import { ResolvedConfig } from "../../core/config.ts";
import { openDb } from "../../core/db.ts";
import { getPeriodCost } from "../../engines/cost.ts";
import { header, fmtUsd, fmtTokens, costBar, outputJson, severity, printCostDisclaimer } from "../format.ts";
interface CostOptions { agent?: string; day?: boolean; week?: boolean; month?: boolean; all?: boolean; json?: boolean; }
export async function runCost(cfg: ResolvedConfig, opts: CostOptions): Promise<void> { const agent = opts.agent ?? cfg.probe.openclaw.agent; const db = openDb(cfg.probeDir); const period: any = opts.day ? "day" : opts.month ? "month" : opts.all ? "all" : "week"; const summary = getPeriodCost(db, agent, period, cfg.probe.cost.customPrices as any); if (opts.json) { outputJson(summary); return; } header("💰", `${period === "day" ? "Today's" : period === "week" ? "Weekly" : period === "month" ? "Monthly" : "All-time"} Cost`, summary.period); console.log(`  Total:     ${fmtUsd(summary.totalUsd)}`); if (period !== "day") { console.log(`  Daily avg: ${fmtUsd(summary.dailyAvg)}`); console.log(`  Month est: ${fmtUsd(summary.monthEstimate)}`); } console.log(); if (summary.daily.length === 0) { console.log(severity.muted("  No cost data yet. Run clawsmith as a daemon to collect data.")); console.log(); return; } const maxUsd = Math.max(...summary.daily.map((d) => d.usd), 0.01); for (const day of summary.daily) { const bar = costBar(day.usd, maxUsd); const usdStr = fmtUsd(day.usd); console.log(`  ${day.date}  ${bar}  ${usdStr}`); } console.log(); const totalCostForPct = Math.max(summary.inputUsd + summary.outputUsd, 0.000001); console.log(`  Input:   ${fmtTokens(summary.inputTokens)} tokens  ${fmtUsd(summary.inputUsd)}  (${Math.round((summary.inputUsd / totalCostForPct) * 100)}%)`); console.log(`  Output:  ${fmtTokens(summary.outputTokens)} tokens  ${fmtUsd(summary.outputUsd)}  (${Math.round((summary.outputUsd / totalCostForPct) * 100)}%)`); if (summary.unpricedModels && summary.unpricedModels.length > 0) { console.log(); console.log(severity.muted(`  ℹ No price data for: ${summary.unpricedModels.join(", ")}`)); } console.log(); printCostDisclaimer(); console.log(); }
