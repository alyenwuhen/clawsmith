import { ResolvedConfig } from "../../core/config.ts";
import { openDb } from "../../core/db.ts";
import { getActiveSession, findJsonlPath } from "../../core/session-store.ts";
import { parseSessionStats } from "../../core/jsonl-parser.ts";
import { analyzeWorkspaceFiles } from "../../engines/file-analyzer.ts";
import { header, fmtTokens, tokenBar, outputJson, severity, getWindowSize } from "../format.ts";
interface ContextOptions { agent?: string; json?: boolean; }
export async function runContext(cfg: ResolvedConfig, opts: ContextOptions): Promise<void> { const agent = opts.agent ?? cfg.probe.openclaw.agent; openDb(cfg.probeDir); const activeSession = getActiveSession(cfg.sessionsDir); const transcriptPath = activeSession ? findJsonlPath(cfg.sessionsDir, activeSession) : null; const jsonlStats = transcriptPath ? parseSessionStats(transcriptPath) : null; const sessionTokens = jsonlStats?.lastTotalTokens ?? activeSession?.sessionTokens ?? 0; const windowSize = (activeSession?.windowSize || activeSession?.contextTokens) ?? 0; const model = jsonlStats?.model ?? activeSession?.modelOverride ?? null; const resolvedWindowSize = getWindowSize(model, windowSize || sessionTokens); const analysis = analyzeWorkspaceFiles(cfg.workspaceDir, cfg.bootstrapMaxChars); const wsTotalInjectedTokens = analysis.files.reduce((s, f) => s + f.injectedEstTokens, 0); const sessionHistoryTokens = sessionTokens > wsTotalInjectedTokens ? sessionTokens - wsTotalInjectedTokens : sessionTokens > 0 ? sessionTokens : 0; if (opts.json) { outputJson({ agent, model, sessionTokens, windowSize: resolvedWindowSize, utilizationPct: resolvedWindowSize > 0 ? Math.round(sessionTokens / resolvedWindowSize * 100) : 0, workspaceOverheadTokensEst: wsTotalInjectedTokens, sessionHistoryTokensEst: sessionHistoryTokens, truncatedFiles: analysis.truncatedFiles.map((f) => ({ name: f.name, lostPct: Math.round(f.lostPercent) })) }); return; } header("🔍", "Context Window", `agent: ${agent}`); if (sessionTokens > 0) { const pct = Math.round(sessionTokens / resolvedWindowSize * 100); console.log(`  Used:    ${fmtTokens(sessionTokens)} / ${fmtTokens(resolvedWindowSize)} tokens  ${tokenBar(sessionTokens, resolvedWindowSize)}  ${pct}%`); } else { console.log(severity.muted(`  Context window: ${fmtTokens(resolvedWindowSize)} tokens  (no session data yet)`)); console.log(); return; } console.log(); console.log(`  Workspace overhead:  ~${fmtTokens(wsTotalInjectedTokens)} tokens`); console.log(`  Conversation est:    ~${fmtTokens(sessionHistoryTokens)} tokens`); if (analysis.truncatedFiles.length > 0) { console.log(); for (const f of analysis.truncatedFiles) console.log(severity.warning(`  ⚠ ${f.name}: ${Math.round(f.lostPercent)}% truncated — model never sees this content`)); } if (resolvedWindowSize > 0) { const remaining = resolvedWindowSize - sessionTokens; const remainingPct = Math.round(remaining / resolvedWindowSize * 100); console.log(); console.log(`  Remaining:  ${fmtTokens(remaining)} tokens (${remainingPct}%)`); } console.log(); }
