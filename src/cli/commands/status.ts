import fs from "fs";
import path from "path";
import { ResolvedConfig } from "../../core/config.ts";
import { openDb, getLatestSnapshot } from "../../core/db.ts";
import { getActiveSession, readSessionsStore, findJsonlPath } from "../../core/session-store.ts";
import { parseSessionStats } from "../../core/jsonl-parser.ts";
import { getPeriodCost } from "../../engines/cost.ts";
import { runRules, persistSuggestions, type ProbeState } from "../../engines/rule-engine.ts";
import { header, fmtTokens, fmtDate, tokenBar, outputJson, severity, getWindowSize } from "../format.ts";
interface StatusOptions { agent?: string; session?: string; json?: boolean; }
function isDaemonRunning(probeDir: string): boolean { const pidFile = path.join(probeDir, "daemon.pid"); if (!fs.existsSync(pidFile)) return false; try { const pid = parseInt(fs.readFileSync(pidFile, "utf-8").trim(), 10); if (isNaN(pid)) return false; process.kill(pid, 0); return true; } catch { return false; } }
export async function runStatus(cfg: ResolvedConfig, opts: StatusOptions): Promise<void> { const agent = opts.agent ?? cfg.probe.openclaw.agent; const db = openDb(cfg.probeDir); let sessionEntry = opts.session ? readSessionsStore(cfg.sessionsDir).find((s) => s.sessionKey === opts.session) : getActiveSession(cfg.sessionsDir); const snapshot = sessionEntry ? getLatestSnapshot(db, agent, sessionEntry.sessionKey) : null; const transcriptPath = sessionEntry ? findJsonlPath(cfg.sessionsDir, sessionEntry) : null; const jsonlStats = transcriptPath ? parseSessionStats(transcriptPath) : null; const sessionTokens = jsonlStats?.lastTotalTokens ?? sessionEntry?.sessionTokens ?? 0; const windowSize = (sessionEntry?.windowSize || sessionEntry?.contextTokens) ?? 0; let compactionCount = jsonlStats?.compactionCount ?? sessionEntry?.compactionCount ?? 0; let lastActiveAt = jsonlStats?.lastActiveAt ?? sessionEntry?.updatedAt ?? 0; if (lastActiveAt === 0 && transcriptPath) { try { lastActiveAt = Math.floor(fs.statSync(transcriptPath).mtimeMs / 1000); } catch {} } const model = snapshot?.model ?? sessionEntry?.modelOverride ?? null; const resolvedWindowSize = getWindowSize(model, windowSize || sessionTokens); const displayContextTokens = sessionTokens > 0 ? sessionTokens : 0; const utilization = resolvedWindowSize > 0 ? displayContextTokens / resolvedWindowSize : 0; const isActive = !opts.session; const todaySummary = getPeriodCost(db, agent, "day", cfg.probe.cost.customPrices as any); const state: ProbeState = { db, agent, workspaceDir: cfg.workspaceDir, sessionsDir: cfg.sessionsDir, bootstrapMaxChars: cfg.bootstrapMaxChars, config: cfg.probe }; const suggestions = runRules(state); persistSuggestions(db, agent, suggestions); const daemonRunning = isDaemonRunning(cfg.probeDir); if (opts.json) { outputJson({ agent, daemonRunning, sessionKey: sessionEntry?.sessionKey ?? null, sessionId: sessionEntry?.sessionId ?? null, model, provider: snapshot?.provider ?? sessionEntry?.providerOverride ?? null, sessionTokens: displayContextTokens, windowSize: resolvedWindowSize, utilizationPct: Math.round(utilization * 100), inputTokens: sessionEntry?.inputTokens ?? 0, outputTokens: sessionEntry?.outputTokens ?? 0, compactionCount, lastActiveAt, isActive, todayUsd: todaySummary.totalUsd, suggestions: suggestions.map((s) => ({ severity: s.severity, ruleId: s.ruleId, title: s.title, detail: s.detail, action: s.action ?? null })) }); return; } header("📊", "Agent Status", isActive ? "(active session)" : `session: ${sessionEntry?.sessionKey}`); console.log(daemonRunning ? severity.ok("  ● daemon running") : severity.warning("  ○ daemon not running  →  run: clawsmith start")); if (!sessionEntry) { console.log(); console.log(severity.muted("  No active session. Start OpenClaw and run a turn to populate data.")); console.log(); return; } console.log(); console.log(`  Agent:     ${severity.bold(agent)}`); console.log(`  Session:   ${severity.muted(sessionEntry.sessionKey)}${isActive ? " ●" : ""}`); if (model) console.log(`  Model:     ${model}`); if (lastActiveAt) console.log(`  Active:    ${fmtDate(lastActiveAt)}`); console.log(`  Compacts:  ${compactionCount}`); console.log(); if (displayContextTokens > 0) { const pct = Math.round(utilization * 100); const bar = tokenBar(displayContextTokens, resolvedWindowSize); console.log(`  Context:   ${fmtTokens(displayContextTokens)} / ${fmtTokens(resolvedWindowSize)}  ${bar}  ${pct}%`); } else { console.log(`  Context:   ${severity.muted("n/a")}`); } console.log(`  Tokens:    ${fmtTokens(sessionEntry.inputTokens)} in / ${fmtTokens(sessionEntry.outputTokens)} out`); console.log(); if (todaySummary.totalUsd > 0) console.log(`  Today:     $${todaySummary.totalUsd.toFixed(2)}  ${severity.muted("→ clawsmith cost for full breakdown")}`); if (suggestions.length > 0) { console.log(); for (const s of suggestions.slice(0, 3)) { const icon = s.severity === "critical" ? "🔴" : s.severity === "warning" ? "🟡" : "🔵"; console.log(`  ${icon}  ${s.title}`); } console.log(severity.muted(`     →  clawsmith suggest  for details & actions`)); } console.log(); }
