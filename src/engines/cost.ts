import { DatabaseSync } from "node:sqlite";
import { getTurnRows, upsertTurnRecord } from "../core/db.ts";
import type { SessionEntry } from "../core/session-store.ts";
import type { SessionStats } from "../core/jsonl-parser.ts";
export interface ModelPrice { input: number; output: number; cacheReadMultiplier?: number; cacheWriteMultiplier?: number; }
export const MODEL_PRICES: Record<string, ModelPrice> = { "anthropic/claude-opus-4": { input: 15, output: 75, cacheWriteMultiplier: 1.25, cacheReadMultiplier: 0.1 }, "anthropic/claude-opus-4.6": { input: 5, output: 25, cacheWriteMultiplier: 1.25, cacheReadMultiplier: 0.1 }, "anthropic/claude-sonnet-4.5": { input: 3, output: 15, cacheWriteMultiplier: 1.25, cacheReadMultiplier: 0.1 }, "anthropic/claude-sonnet-4.6": { input: 3, output: 15, cacheWriteMultiplier: 1.25, cacheReadMultiplier: 0.1 }, "openai/gpt-4o": { input: 2.5, output: 10, cacheReadMultiplier: 0.5, cacheWriteMultiplier: 0 }, "openai/gpt-4o-mini": { input: 0.15, output: 0.6, cacheReadMultiplier: 0.5, cacheWriteMultiplier: 0 }, "openai/o3": { input: 2, output: 8, cacheReadMultiplier: 0.5, cacheWriteMultiplier: 0 }, "openai/gpt-5.4": { input: 5, output: 20, cacheReadMultiplier: 0.5, cacheWriteMultiplier: 0 }, "openai/gpt-5.4-mini": { input: 0.3, output: 1.2, cacheReadMultiplier: 0.5, cacheWriteMultiplier: 0 }, "openai/o4-mini": { input: 1.1, output: 4.4, cacheReadMultiplier: 0.5, cacheWriteMultiplier: 0 }, "google/gemini-2.5-flash": { input: 0.3, output: 2.5, cacheReadMultiplier: 0.25, cacheWriteMultiplier: 1 }, "google/gemini-2.5-pro": { input: 1.25, output: 10, cacheReadMultiplier: 0.25, cacheWriteMultiplier: 1 }, "deepseek/deepseek-v3": { input: 0.27, output: 1.1, cacheReadMultiplier: 0.1, cacheWriteMultiplier: 0 }, "deepseek/deepseek-v3.2": { input: 0.28, output: 0.42, cacheReadMultiplier: 0.1, cacheWriteMultiplier: 0 }, "deepseek/deepseek-r1": { input: 0.55, output: 2.19, cacheReadMultiplier: 0.1, cacheWriteMultiplier: 0 }, "moonshot/kimi-k2": { input: 0.4, output: 2, cacheReadMultiplier: 0.167, cacheWriteMultiplier: 0 }, "moonshot/kimi-k2.5": { input: 0.6, output: 2, cacheReadMultiplier: 0.167, cacheWriteMultiplier: 0 }, "moonshot/delivery-mirror": { input: 0.6, output: 2, cacheReadMultiplier: 0.167, cacheWriteMultiplier: 0 }, "qwen/qwen3-max": { input: 0.34, output: 1.38 }, "qwen/qwen3.5-plus": { input: 0.11, output: 0.66 }, "qwen/qwen3.5-flash": { input: 0.065, output: 0.26 } };
export interface TokenCount { input: number; output: number; cacheRead?: number; cacheWrite?: number; }
export interface DailyCost { date: string; usd: number; inputTokens: number; outputTokens: number; }
export interface PeriodCostSummary { period: string; startDate: string; endDate: string; totalUsd: number; inputTokens: number; outputTokens: number; cacheReadTokens: number; cacheWriteTokens: number; inputUsd: number; outputUsd: number; dailyAvg: number; monthEstimate: number; daily: DailyCost[]; model?: string; unpricedModels?: string[]; }
export interface SessionCost { sessionKey: string; model: string | null; provider: string | null; inputTokens: number; outputTokens: number; totalTokens: number; contextTokens: number; isOrphan: boolean; estimatedUsd: number; startedAt: number; lastActiveAt: number; durationMin: number; compactionCount: number; costAccurate: boolean; turns: TurnCost[]; }
export interface TurnCost { turnIndex: number; timestamp: number; inputTokensDelta: number; outputTokensDelta: number; estimatedUsd: number; compactOccurred: boolean; }
export function estimateCost(tokens: TokenCount, model: string | null, customPrices: Record<string, ModelPrice> = {}): number { if (!model) return 0; const prices = { ...MODEL_PRICES, ...customPrices }; const price = prices[model] ?? Object.entries(prices).find(([key]) => model.includes(key.split("/")[1] ?? key))?.[1]; if (!price) return 0; const cacheRead = tokens.cacheRead ?? 0; const cacheWrite = tokens.cacheWrite ?? 0; const normalInput = Math.max(0, tokens.input - cacheRead - cacheWrite); const cacheReadRate = price.cacheReadMultiplier ?? 0; const cacheWriteRate = price.cacheWriteMultiplier ?? 0; return (normalInput / 1_000_000) * price.input + (tokens.output / 1_000_000) * price.output + (cacheRead / 1_000_000) * price.input * cacheReadRate + (cacheWrite / 1_000_000) * price.input * cacheWriteRate; }
export function sessionCostFromEntry(entry: SessionEntry, customPrices: Record<string, { input: number; output: number }> = {}): SessionCost { const model = entry.modelOverride ?? null; const usd = estimateCost({ input: entry.inputTokens, output: entry.outputTokens }, model, customPrices as any); return { sessionKey: entry.sessionKey, model, provider: entry.providerOverride ?? null, inputTokens: entry.inputTokens, outputTokens: entry.outputTokens, totalTokens: entry.totalTokens, contextTokens: entry.sessionTokens ?? 0, isOrphan: false, estimatedUsd: usd, startedAt: entry.updatedAt, lastActiveAt: entry.updatedAt, durationMin: 0, compactionCount: entry.compactionCount, costAccurate: false, turns: [] }; }
export function getSessionCostFromJsonl(stats: SessionStats, sessionKey: string, customPrices: Record<string, { input: number; output: number }> = {}): SessionCost { const model = stats.model; const turns: TurnCost[] = stats.turns.map((t) => ({ turnIndex: t.turnIndex, timestamp: t.timestamp, inputTokensDelta: t.usage.input, outputTokensDelta: t.usage.output, estimatedUsd: estimateCost({ input: t.usage.input, output: t.usage.output, cacheRead: t.usage.cacheRead, cacheWrite: t.usage.cacheWrite }, model, customPrices as any), compactOccurred: false })); const totalUsd = turns.reduce((s, t) => s + t.estimatedUsd, 0); return { sessionKey, model, provider: stats.provider, inputTokens: stats.totalInput, outputTokens: stats.totalOutput, totalTokens: stats.totalInput + stats.totalOutput, contextTokens: stats.lastTotalTokens, isOrphan: false, estimatedUsd: totalUsd, startedAt: stats.startedAt, lastActiveAt: stats.lastActiveAt, durationMin: Math.round((stats.lastActiveAt - stats.startedAt) / 60), compactionCount: stats.compactionCount, costAccurate: true, turns }; }
export function recordSessionTurns(db: DatabaseSync, agent: string, sessionKey: string, stats: SessionStats, customPrices: Record<string, ModelPrice> = {}): void { for (const turn of stats.turns) { const turnDate = turn.timestamp > 0 ? new Date(turn.timestamp * 1000).toISOString().slice(0, 10) : todayString(); const usd = estimateCost({ input: turn.usage.input, output: turn.usage.output, cacheRead: turn.usage.cacheRead, cacheWrite: turn.usage.cacheWrite }, turn.model ?? stats.model, customPrices); upsertTurnRecord(db, { agent, session_key: sessionKey, date: turnDate, turn_index: turn.turnIndex, sampled_at: turn.timestamp > 0 ? turn.timestamp : Math.floor(Date.now() / 1000), model: turn.model ?? stats.model, provider: turn.provider ?? stats.provider, input_tokens: turn.usage.input, output_tokens: turn.usage.output, cache_read: turn.usage.cacheRead, cache_write: turn.usage.cacheWrite, estimated_usd: usd }); } }
export function getPeriodCost(db: DatabaseSync, agent: string, period: "day" | "week" | "month" | "all", customPrices: Record<string, ModelPrice> = {}): PeriodCostSummary { const now = new Date(); const today = formatDate(now); let days: number; let startDate: string; let periodLabel: string; switch (period) { case "day": days = 1; startDate = today; periodLabel = `Today (${today})`; break; case "week": days = 7; startDate = formatDate(new Date(now.getTime() - 6 * 86400_000)); periodLabel = `${startDate} – ${today}`; break; case "month": days = 30; startDate = formatDate(new Date(now.getTime() - 29 * 86400_000)); periodLabel = `${startDate} – ${today}`; break; default: days = 3650; startDate = "2020-01-01"; periodLabel = "All time"; } const rows = getTurnRows(db, agent, days); const byDate = new Map<string, { usd: number; inputUsd: number; outputUsd: number; input: number; output: number; cacheRead: number; cacheWrite: number; unpriced: string[] }>(); for (const row of rows) { let entry = byDate.get(row.date); if (!entry) { entry = { usd: 0, inputUsd: 0, outputUsd: 0, input: 0, output: 0, cacheRead: 0, cacheWrite: 0, unpriced: [] }; byDate.set(row.date, entry); } entry.input += row.input_tokens; entry.output += row.output_tokens; entry.cacheRead += row.cache_read; entry.cacheWrite += row.cache_write; if (row.model) { const inputOnly = estimateCost({ input: row.input_tokens, output: 0, cacheRead: row.cache_read, cacheWrite: row.cache_write }, row.model, customPrices); const outputOnly = estimateCost({ input: 0, output: row.output_tokens }, row.model, customPrices); const recomputed = inputOnly + outputOnly; if (recomputed > 0) { entry.usd += recomputed; entry.inputUsd += inputOnly; entry.outputUsd += outputOnly; } else { entry.usd += row.estimated_usd; const total = row.input_tokens + row.output_tokens; if (total > 0) { entry.inputUsd += row.estimated_usd * (row.input_tokens / total); entry.outputUsd += row.estimated_usd * (row.output_tokens / total); } if (!entry.unpriced.includes(row.model)) entry.unpriced.push(row.model); } } else { entry.usd += row.estimated_usd; } } const unpricedModels = new Set<string>(); for (const entry of byDate.values()) entry.unpriced.forEach((m) => unpricedModels.add(m)); const daily: DailyCost[] = [...byDate.entries()].map(([date, entry]) => ({ date, usd: entry.usd, inputTokens: entry.input, outputTokens: entry.output })).sort((a, b) => a.date.localeCompare(b.date)); const totalUsd = daily.reduce((s, d) => s + d.usd, 0); const totalInput = daily.reduce((s, d) => s + d.inputTokens, 0); const totalOutput = daily.reduce((s, d) => s + d.outputTokens, 0); const totalCacheRead = [...byDate.values()].reduce((s, e) => s + e.cacheRead, 0); const totalCacheWrite = [...byDate.values()].reduce((s, e) => s + e.cacheWrite, 0); const totalInputUsd = [...byDate.values()].reduce((s, e) => s + e.inputUsd, 0); const totalOutputUsd = [...byDate.values()].reduce((s, e) => s + e.outputUsd, 0); const spanDays = period === "all" ? Math.max(daily.length, 1) : days; const dailyAvg = totalUsd / Math.max(spanDays, 1); return { period: periodLabel, startDate, endDate: today, totalUsd, inputTokens: totalInput, outputTokens: totalOutput, cacheReadTokens: totalCacheRead, cacheWriteTokens: totalCacheWrite, inputUsd: totalInputUsd, outputUsd: totalOutputUsd, dailyAvg, monthEstimate: dailyAvg * 30, daily, unpricedModels: unpricedModels.size > 0 ? [...unpricedModels] : undefined }; }
export function formatDate(d: Date): string { return d.toISOString().slice(0, 10); }
export function todayString(): string { return formatDate(new Date()); }
